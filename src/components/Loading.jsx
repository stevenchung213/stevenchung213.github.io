import React from 'react';
import ReactLoading from 'react-loading';

const Loading = () => (
  <ReactLoading
    type={'spinningBubbles'} color={'#ff0000'}
    height={'10%'} width={'10%'}/>
);

export default Loading;